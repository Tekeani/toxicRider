import { Scene } from './Scene';
import type { Renderer } from '../core/Renderer';
import type { Keys } from '../types';
import type { Game } from '../core/Game';
export declare class RoguelikeScene extends Scene {
    private game;
    private player;
    private enemies;
    private currentWave;
    private waves;
    private allWavesComplete;
    private tileMap;
    private tilesLoaded;
    private attackFeedbacks;
    private insults;
    private insultList;
    private usedInsults;
    private waveStartTimer;
    private waveStartDelay;
    private enemyAttackCooldown;
    private enemyAttackIndex;
    private _attackPressed;
    private _toxicityPressed;
    private upgradeMenuActive;
    private gameOver;
    private buttonPressed;
    private upgradeButtonPressed;
    private cleanupInput;
    private renderingSystem;
    private clickHandler;
    constructor(game: Game);
    private setupInput;
    init(): Promise<void>;
    private spawnWave;
    private playerAttack;
    private useToxicity;
    private upgradeStat;
    update(deltaTime: number, keys: Keys): void;
    render(renderer: Renderer): void;
    private renderBackground;
    private renderTrees;
    private renderRocks;
    private renderCountdown;
    private renderFeedbacks;
    private renderInsults;
    private renderUpgradeMenu;
    private renderGameOver;
    private handleClick;
    private restartWave;
    cleanup(): void;
}
//# sourceMappingURL=RoguelikeScene.d.ts.map